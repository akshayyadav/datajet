<div class="container">
  <h3>Diesel Record for:
    <%= @diesel.reading_at %>
  </h3>
  <div class="table-responsive">
    <table class="table table-hover table-condensed">
      <thead>
        <tr>
          <th>Date</th>
          <th>Opening Stock</th>
          <th>Received Stock</th>
          <th>Actual Stock</th>
          <th>Total Stock</th>
          <th>Closing Stock</th>
          <th>Opening Meter</th>
          <th>Testing Meter</th>
          <th>Closing Meter</th>
          <th>Actual Sale</th>
          <th>Purchase Rate</th>
          <th>Sale Rate</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= @diesel.reading_at %></td>
          <td><%= @diesel.opening_stock %></td>
          <td><%= @diesel.received_stock %></td>
          <td><%= @diesel.actual_stock %></td>
          <td><%= @diesel.total_stock %></td>
          <td><%= @diesel.closing_stock %></td>
          <td><%= @diesel.opening_meter %></td>
          <td><%= @diesel.testing_meter %></td>
          <td><%= @diesel.closing_meter %></td>
          <td><%= @diesel.actual_sale %></td>
          <td><%= @diesel.purchase_rate %></td>
          <td><%= @diesel.sale_rate %></td>
        </tr>
      </tbody>
    </table>
  </div>

  <div class='col-md-2'>
    <%= link_to edit_diesel_path(@diesel), class: "btn btn-warning btn-sm" do %>
      <i class="glyphicon glyphicon-pencil"></i> Edit
    <% end %>
    <%= link_to diesel_path(@diesel), class: "btn btn-danger btn-sm",
      method: :delete,
      data: { confirm: 'Are you sure?' } do %>
      <i class="glyphicon glyphicon-remove"></i> Delete
    <% end %>
  </div>
  <div>
    <%= link_to diesels_path, class: "btn btn-info btn-sm" do %>
      <i class="glyphicon glyphicon-arrow-left"></i> Back
    <% end %>
  </div>
</div>
